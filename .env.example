# Copy this to frontend/.env.local for local development
# pointing at a remote backend instead of localhost.
# In Vercel: set NEXT_PUBLIC_API_URL in the project's Environment Variables dashboard.

NEXT_PUBLIC_API_URL=https://your-render-service.onrender.com
